Android 开发规范

总则与优先级

· 在不违反本规范的情况下，遵循官方推荐做法与社区最佳实践。
· 平台/框架 API 一律查官方文档或源码后再写，禁止凭记忆使用已废弃或已移除的接口。
· 本规范为项目变更准则，偏离需有明确理由。
· 若用户决策调整或需求发生变更，请据此对本开发文档进行相应修订，使其与项目演进保持一致。

核心决策

· 语言：Kotlin
· 计算：Rust（按需）
· 界面：Jetpack Compose
· 依赖注入：手动注入（必须）
· 导航框架：navigation3

注释规范

· KDoc：仅用于 public/internal API 表面。
· 私有实现：使用 // 双斜杠注释。
· 注释内容：解释代码意图与设计决策，不复述代码行为。
· 同步要求：代码逻辑变更时，相关注释必须同步更新；未随代码更新的注释视为缺陷。
· 禁止内容：溯源、背景等无关信息。
· TODO 格式：// TODO(owner/issue): 说明。

代码组织

· 同职责内聚，异职责解耦。
· 控制文件碎片化：同一职责不得四散，一个功能需翻阅多个文件、甚至跨层文件才能拼出全貌，即不符合预期。
· 组件位置：跨页面复用上提至 ui/component/；无跨页面复用下沉到页面 component/。
· 子目录采用语义命名，父目录采用泛义命名：父目录表示这里可能有什么，子目录则明确是什么。
· 页面根包禁止平铺骨架以外文件。仅允许按需创建子目录收敛页面所需的其它业务。
· 页面骨架白名单：XxxScreen.kt、XxxUiState.kt、XxxViewModel.kt。其余文件必须进入 component/、compact/、medium/、expanded/ 等子目录。
· 组件上提标准：跨 2 个及以上页面复用，或属于统一布局风格的通用组件（对话框、弹出提示、错误横幅、开关按钮等），才可上提至 ui/component/。
· 一个文件可包含多个紧密相关的私有 Composable，不必过度碎片化。
· utils/ 必须限定为无状态、纯函数、无 Android 依赖；否则归入对应层。
· data/ 建议细分：remote/、local/、repository/、model/、mapper/。
· 是否引入 domain/ 根据项目实际情况判断。不违背控制碎片化则可引入若违背则不予引入。
· MainViewModel 要限定职责，避免变成 Activity 级“上帝对象”。

性能与并发

· 缓存昂贵计算结果并提供稳定唯一键；状态管理遵循官方最佳实践。
· 耗时任务采用协程 + 结构化并发切离主线程；可并行任务充分并发，避免串行和阻塞主 UI。
· 禁止 GlobalScope、主线程 runBlocking。
· Dispatcher 应可注入，便于测试。
· 使用结构化并发，明确异常处理、取消传播、超时。
· 可并行任务用 async + awaitAll，避免无意义串行。

状态管理

· 页面状态统一 StateFlow<UiState>。
· Compose 侧用 collectAsStateWithLifecycle()。
· 一次性事件用 Channel 或 SharedFlow，不要塞进 UiState。
· UiState 必须不可变，列表项提供稳定唯一键。
· 选中项用稳定 ID，不用索引。
· 状态保持与恢复：布局切换时，必须明确焦点转移规则和选中状态的保持与恢复策略。
· 选中状态：保存稳定 ID。
· 焦点：明确焦点转移规则，使用 FocusRequester。
· 滚动：LazyListState 可保存。
· 输入：rememberSaveable。
· 导航回退栈、播放进度、列表位置都要明确恢复策略。

导航与自适应

· 布局分发：基于 WindowSizeClass 和必要的显示控制（如播放器或游戏通常有竖屏和横屏两种情况）进行。旋转状态变化会触发 WindowSizeClass 重新计算，无需单独监听旋转事件。
· 折叠、多窗口、自由窗口仍应依赖 WindowSizeClass / WindowLayoutInfo。

设计决策

· 页面根包禁止平铺骨架以外文件。仅允许按需创建子目录收敛页面所需的其它业务。
· UI 组件原子化，由组装器组装页面 component/ 或 ui/component/ 下的组件，以显示不同布局。
· 布局分发：基于 WindowSizeClass 和必要的显示控制进行。
· 状态保持与恢复：布局切换时，必须明确焦点转移规则和选中状态的保持与恢复策略。

页面模块目录示例

```text
screens/home/
├── compact/                       # 竖屏组装
│   ├── HomeCompactAssembly.kt     # 竖屏组装器
│   └── …/                         # 竖屏专属组件
│       └── ….kt
├── medium/                        # 横屏组装
│   └── HomeMediumAssembly.kt      # 横屏组装器
├── expanded/                      # 平板组装
│   └── HomeExpandedAssembly.kt    # 平板组装器
├── component/                     # 页面组件
│   ├── welcome/
│   │   └── Welcome.kt
│   └── …/
├── HomeScreen.kt
├── HomeUiState.kt
└── HomeViewModel.kt
```

手动依赖注入

· AppContainer 只放应用级单例。
· 页面级依赖通过 XxxViewModelFactory 或页面级容器创建。
· 禁止 Composable 直接引用 AppContainer。
· 明确作用域：Singleton、ViewModel、Activity、Composable。

顶层目录结构示例

```text
com/template/evilgodxu/
├── data/                 # 数据层
├── log/                  # 异常日志与崩溃采集
├── navigation/           # 导航宿主与路由定义
├── screens/              # 页面层：XxxScreen + ViewModel + UiState + 形态组装器
├── theme/                # 主题：颜色、字体、整体主题
├── localization/         # 语言本地化管理
├── permission/           # 权限状态管理器（运行时权限与特殊权限统一出口）
├── windowsize/           # 窗口尺寸类判定
├── ui/                   # 跨页面复用组件与通用 UI 能力
│   ├── component/        # 共享 UI 可组合项（统一收纳，按语义细分）
│   │   ├── dialog/       # 通用对话框
│   │   └── ...           # 其它按语义细分的可组合项
│   └── icons/            # 图标资源封装
├── update/               # 应用更新与下载
├── utils/                # 通用工具
├── App.kt                # Application 入口
├── AppContainer.kt       # 手动 DI 容器（应用级单例）
├── AppUiState.kt         # 应用级 UI 状态（MainViewModel 对外状态类型）
├── MainActivity.kt       # 唯一 Activity
└── MainViewModel.kt      # Activity 专属 ViewModel
```

国际化与文案

· 用户可见文案一律入资源文件：默认 values/strings.xml（中文）+ values-en/strings.xml（英文），禁止在 Kotlin、资源 XML、Manifest 中硬编码。
· 新增或修改文案必须中英同步，禁止只改一侧；占位符、格式与标点需两边一致。
· Compose 层统一用 stringResource 取文案；非 Compose 上下文（Service、通知、广播接收器、Application 等）必须按应用内语言构造本地化 Context 后再取资源，不得用系统 Context 取用户可见文案。
· 服务端下发的动态内容（如更新说明）不属硬编码范畴；需多语言时由数据源提供多语言字段，客户端按当前语言选择。
· 例外：不面向用户的字符串（日志正文、异常信息、断言信息）可不入资源。

安全与隐私

· 权限申请必须遵循最小必要原则和权限状态管理器。

Rust 使用规范

· Rust 按需使用，标准：性能敏感、跨平台复用、加密、算法等。
· 明确 JNI/UniFFI、ABI、线程、错误映射、构建集成。
· Rust 与 Kotlin 边界必须有统一错误映射和线程模型说明。
